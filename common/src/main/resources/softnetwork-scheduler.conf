softnetwork{
  scheduler {
    path = "scheduler"
    # id = "*"
    akka-node-role = scheduler
    reset-scheduler {
      initial-delay = 60
      delay = 60
    }
    event-streams {
      entity-to-scheduler-tag = "entity-to-scheduler"
    }
  }
}
