softnetwork{
  scheduler {
    id = "my-scheduler"
    reset-scheduler {
      initial-delay = 0
      delay = 1
    }
    event-streams {
      entity-to-scheduler-tag = "entity-to-scheduler-"${softnetwork.scheduler.id}
    }
  }
}